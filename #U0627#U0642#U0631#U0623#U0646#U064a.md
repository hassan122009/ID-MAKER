# VexLap ID Maker — نسخة الويب (PWA / أوفلاين بالكامل)

تطبيق إصدار بطاقات الهوية، شغال كصفحة ويب عادية وقابل للتثبيت كبرنامج (PWA)، وكل المعالجة بتحصل في المتصفح — مفيش أي بيانات بترفع لأي سيرفر.

**مفيش ولا مكتبة بتتحمل من الإنترنت وقت التشغيل.** كل المكتبات (Tailwind, Font Awesome, خط Cairo, xlsx, jszip, FileSaver) اتحملت وضُمّنت جوه مجلد `vendor/` بنفس النسخ اللي كانت متحملة من الـ CDN قبل كده. أول مرة تفتح فيها التطبيق (أونلاين أو من نفس الجهاز)، الـ Service Worker بيعمل تخزين كامل لكل الملفات دي (37 ملف تقريبًا) — وبعدها التطبيق يفضل شغال 100% حتى من غير نت خالص، حتى لو قفلت المتصفح وفتحته تاني بعد أيام.

## رفعه على GitHub

ارفع محتويات المجلد ده (كل الملفات اللي جواه) لريبو جديد. لو عايز تستضيفه مجانًا كموقع فعلي:
1. من إعدادات الريبو (Settings) → Pages
2. اختار الفرع (branch) اللي رفعت عليه، والمجلد `/ (root)`
3. هيديك رابط شكله `https://username.github.io/repo-name/`

الرابط ده هيشتغل أوفلاين ويتثبت زي أي PWA عادي، لأن GitHub Pages بيشتغل على https افتراضيًا (شرط أساسي لعمل الـ Service Worker والتثبيت).

## تشغيله محليًا (localhost) وقت التطوير

### الأسهل (ويندوز)
دبل كليك على `start_windows.bat` — هيفتح المتصفح تلقائيًا على `http://localhost:8080`.

### يدويًا
```
node server.js
```
أو
```
python -m http.server 8080
```

> لازم تفتحه عن طريق `http://localhost` أو `https` (زي GitHub Pages) مش بالدبل كليك المباشر على `index.html`، لأن الـ Service Worker (اللي مسؤول عن التخزين والتثبيت) مبيشتغلش من `file://`.

## تثبيت التطبيق كبرنامج (PWA)

بعد ما تفتح الرابط (localhost أو GitHub Pages) في **Chrome** أو **Edge**:
- على الكمبيوتر: أيقونة تثبيت ⊕ في شريط العنوان.
- على أندرويد (Chrome): قائمة ⋮ ← "تثبيت التطبيق" / "إضافة إلى الشاشة الرئيسية".
- على آيفون (Safari): زر المشاركة ← "إضافة إلى الشاشة الرئيسية".

## محتويات المجلد
```
index.html            التطبيق نفسه
manifest.json          إعدادات التثبيت (الاسم، الأيقونة، الألوان)
service-worker.js       تخزين كل الملفات أوفلاين عند أول فتحة
icons/                  أيقونات التطبيق
vendor/                 كل المكتبات مُحمّلة محليًا (Tailwind، Font Awesome، خط Cairo، xlsx، jszip، FileSaver)
server.js / start_windows.bat   تشغيل سيرفر محلي وقت التطوير فقط (مش مطلوب على GitHub Pages)
```

## تحديث نسخة الملفات لاحقًا
لو عدّلت أي حاجة في `index.html` بعد كده، لازم تزود رقم النسخة في أول `service-worker.js`:
```js
const CACHE_VERSION = 'vexlap-id-maker-v3';   // غيّرها لـ v4 مثلاً
```
عشان المتصفح يحدّث النسخة المخزنة عند الزوار اللي عندهم نسخة قديمة متخزنة.

## استبدال الأيقونة باللوجو الحقيقي
الأيقونة الحالية (`icons/`) مؤقتة بألوان البراند (كحلي وبرتقالي). لو عندك ملف اللوجو الأصلي، ابعته وهنولّده في كل المقاسات المطلوبة (192px, 512px, وiOS 180px) ونحطه مكان المؤقتة تلقائيًا.
